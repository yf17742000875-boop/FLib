### 11:06 配置对话结束笔记规则

- 结论：已在 `AGENTS.md` 增加“对话结束笔记规则”，要求将可复用的部署、训练、下载、排错、实验、配置或项目决策记录到 `notes/`。
- 关键操作：新增规则区分简短日记 `notes/YYYY-MM-DD.md` 与完整 runbook，例如 AutoDL Cosmos3-Super 双 Pro6000 部署流程可单独整理为 `notes/autodl-cosmos3-super-2x-pro6000.md`。
- 涉及文件：`AGENTS.md`、`notes/2026-07-07.md`。
- 后续：后续任务跑通后，结束前应说明笔记写入路径和核心结论。

### 23:10 vLLM-Omni 与 Cosmos3 本地/云端学习部署路线

#### 目标

- 学习 vLLM-Omni 的本地开发、Docker 启动、OpenAI 兼容接口调用，以及在云服务器上部署 Cosmos3。
- 本地硬件按 RTX 5070 Ti 16GB 估算，云端按 RTX PRO 6000 Blackwell 96GB 单卡估算。

#### 环境

- 已验证资料来源：vLLM-Omni 官方 quickstart/GPU 安装页、NVIDIA Cosmos GitHub README、vLLM Cosmos3 recipe、NVIDIA/Hugging Face 模型卡。
- 已验证结论：
  - vLLM-Omni 当前要求 Linux + Python 3.12，Windows 不原生支持；本地 Windows 应通过 WSL2 Ubuntu 或 Linux Docker 学习。
  - RTX 5070 Ti 只有 16GB 显存，不适合作为 Cosmos3-Nano/Super 的实际部署目标，更适合学习 Docker、API、客户端和小模型 quickstart。
  - RTX PRO 6000 Blackwell 96GB 更适合作为 Cosmos3-Nano Generator 的首个云端目标。
  - Cosmos3-Super / Super-Text2Image 官方 recipe 面向 8xH100 或 4xH200/4xGB200 等多卡配置；单张 PRO 6000 不作为首选部署目标。
- 未验证假设：
  - 单卡 PRO 6000 跑 Cosmos3-Nano 可用，但具体速度、峰值显存、是否需要禁用 guardrails 需实测。
  - 单卡 PRO 6000 加 `--enable-layerwise-offload` 或量化尝试 Super 属实验路径，不作为稳定方案。

#### 目录规划

- 本地 WSL2：`~/models/hf-cache`、`~/work/vllm-omni`
- 云端 Linux：`/data/hf-cache`、`~/cosmos3-work`
- Hugging Face 模型缓存通过 `HF_HOME` 或 Docker volume 挂载到容器内 `/root/.cache/huggingface`。

#### 镜像/依赖

- vLLM-Omni quickstart：`vllm/vllm-omni:v0.24.0`
- Cosmos3 推荐镜像：`vllm/vllm-omni:cosmos3`
- Python 本地源码路径优先用 `uv`，但 Cosmos3 部署优先用 Docker 降低 CUDA/依赖摩擦。

#### 模型来源

- vLLM-Omni quickstart 小模型：`Tongyi-MAI/Z-Image-Turbo`
- Cosmos3 云端首测模型：`nvidia/Cosmos3-Nano`
- Super 备选模型：`nvidia/Cosmos3-Super`、`nvidia/Cosmos3-Super-Text2Image`、`nvidia/Cosmos3-Super-Image2Video`
- Guardrail：`nvidia/Cosmos-1.0-Guardrail`，需要 Hugging Face gated repo 权限；学习阶段可按官方 recipe 关闭 guardrails，但需要自行承担合规责任。

#### 关键命令

本地 Windows 只做 WSL2/Docker 准备：

```cmd
wsl --status
nvidia-smi
docker version
```

WSL2/云端 Linux 通用缓存准备：

```bash
export HF_TOKEN=hf_xxx
export HF_HOME=/data/hf-cache
mkdir -p "$HF_HOME" ~/cosmos3-work
```

云端 PRO 6000 首选启动 Cosmos3-Nano：

```bash
docker pull vllm/vllm-omni:cosmos3
docker run --rm --name cosmos3-nano \
  --runtime nvidia --gpus all \
  -v "$HF_HOME:/root/.cache/huggingface" \
  -v "$HOME/cosmos3-work:/workspace" \
  -e "HF_TOKEN=$HF_TOKEN" \
  -p 8000:8000 \
  --ipc=host \
  vllm/vllm-omni:cosmos3 \
  vllm serve nvidia/Cosmos3-Nano \
    --omni \
    --model-class-name Cosmos3OmniDiffusersPipeline \
    --allowed-local-media-path / \
    --host 0.0.0.0 \
    --port 8000 \
    --init-timeout 1800
```

最小验证：

```bash
curl http://127.0.0.1:8000/v1/models
curl -sS -X POST http://127.0.0.1:8000/v1/videos/sync \
  -H "Accept: video/mp4" \
  --form-string "model=nvidia/Cosmos3-Nano" \
  --form-string "prompt=A small warehouse robot moves a blue box across a clean floor." \
  --form-string "negative_prompt=blurry, distorted, low quality" \
  --form-string "size=832x480" \
  --form-string "num_frames=49" \
  --form-string "fps=24" \
  --form-string "num_inference_steps=20" \
  --form-string "guidance_scale=6.0" \
  --form-string "flow_shift=10.0" \
  --form-string "seed=0" \
  --form-string 'extra_params={"use_resolution_template":false,"use_duration_template":false,"guardrails":true}' \
  -o cosmos3_nano_smoke.mp4
```

#### 问题-原因-修复

- 问题：Windows 原生 Python 运行 vLLM-Omni。
  - 原因：vLLM-Omni 官方当前不原生支持 Windows。
  - 修复：使用 WSL2 Ubuntu 或云端 Linux；本地 cmd.exe 只负责进入 WSL、管理 Docker Desktop/WSL 集成。
- 问题：5070 Ti 本地跑 Cosmos3 可能 OOM。
  - 原因：5070 Ti 显存约 16GB，Cosmos3-Nano BF16 级别权重和运行峰值远高于 16GB。
  - 修复：本地只跑 quickstart/小模型/客户端；Cosmos3 放到 PRO 6000 云端。
- 问题：启动时报 Guardrail 权限或模型访问错误。
  - 原因：Cosmos3 Generator 默认启用 guardrails，相关 HF repo 是 gated。
  - 修复：申请模型和 Guardrail 权限并设置 `HF_TOKEN`；学习阶段如确认合规，可用官方 recipe 的关闭 guardrail 方案。
- 问题：Super 单卡启动失败或极慢。
  - 原因：官方 Super recipe 是多卡目标，单卡 PRO 6000 不是推荐部署形态。
  - 修复：先跑 Nano；Super 只在多卡 H100/H200/GB200 或足够多 PRO 6000 上按 recipe 做实验。

#### 验证方式

- 服务日志出现 `Application startup complete.`
- `curl http://127.0.0.1:8000/v1/models` 返回模型列表。
- `POST /v1/videos/sync` 成功生成 `cosmos3_nano_smoke.mp4`。
- 监控 `nvidia-smi` 的显存峰值和运行时间，记录是否需要 `--enable-layerwise-offload`、降低分辨率、减少帧数或关闭 guardrails。

#### 最终状态

- 本轮产出的是学习与部署路线，尚未在本机或云端实际运行。
- 推荐下一步：先在本地 WSL2 跑 vLLM-Omni quickstart 熟悉接口，再在云端 PRO 6000 用 Docker 跑 `nvidia/Cosmos3-Nano` 的 480p 短视频 smoke test。

### 13:05 vLLM-Omni 本地 Docker 冒烟实测

#### 目标

- 在本地 Windows + RTX 5070 Ti 上按“小模型冒烟”思路验证 vLLM-Omni Docker、GPU 透传、模型缓存、8091 端口和 OpenAI 兼容接口。
- 优先尝试 `Tongyi-MAI/Z-Image-Turbo`；失败后换小模型排查；最后用同一 vLLM-Omni 镜像的非 `--omni` vLLM 路径验证 Docker/GPU/API 基线。

#### 环境

- 已验证：
  - GPU：NVIDIA GeForce RTX 5070 Ti，显存 16303 MiB。
  - 驱动：591.86，宿主 `nvidia-smi` 显示 CUDA 13.1。
  - WSL：`Ubuntu` 为 WSL2。
  - Docker：Docker Desktop 4.70.0，Engine 29.4.0。
  - 容器内 PyTorch CUDA：`torch 2.11.0+cu130`、CUDA 13.0，可在 `cuda:0` 创建 tensor。
- 未验证：
  - WSL2 原生 Python `uv + vllm-omni` 安装路径未执行。
  - 云端单机多 GPU stage 放置未执行，本轮没有云服务器会话。

#### 目录规划

- 本地 Hugging Face 缓存：`D:\HFModels\hf-cache`
- Docker 挂载：`D:\HFModels\hf-cache:/root/.cache/huggingface`
- 服务端口：宿主 `8091` 映射容器 `8091`

#### 镜像/依赖

- 原计划镜像 `vllm/vllm-omni:v0.24.0` 在 Docker Hub 未找到 tag。
- 实际使用镜像：`vllm/vllm-omni:v0.22.0-x86_64`
- `vllm/vllm-omni:latest` 的 amd64 digest 与 `v0.22.0-x86_64` 相同，因此切到 `latest` 不能修复当前 registry 问题。
- Docker Desktop 当前接受 `--ipc=host`，未触发改用 `--shm-size 16g`。

#### 模型来源

- `Tongyi-MAI/Z-Image-Turbo`：官方 quickstart 目标，但 Hugging Face API 显示仓库约 54GB，不适合作为 16GB 5070 Ti 本地小模型冒烟。
- `stabilityai/stable-diffusion-xl-base-1.0`：仓库约 77GB，也不适合作为本轮本地冒烟。
- `inclusionAI/Ming-omni-tts-0.5B`：仓库约 3.27GB，已下载到缓存，但当前镜像不认识其模型类。
- `Qwen/Qwen2.5-0.5B-Instruct`：用于同一镜像的非 `--omni` vLLM 基线服务验证。

#### 关键命令

本地预检：

```cmd
nvidia-smi
wsl -l -v
docker version
docker run --rm --gpus all vllm/vllm-omni:v0.22.0-x86_64 nvidia-smi
```

启动 Docker Desktop：

```cmd
powershell -NoProfile -Command Start-Process -FilePath C:\Progra~1\Docker\Docker\DOCKER~2.EXE -WindowStyle Hidden
```

vLLM-Omni 镜像内 CUDA 最小验证：

```cmd
docker run --rm --gpus all vllm/vllm-omni:v0.22.0-x86_64 python3 -c t=__import__('torch');print(t.__version__,t.version.cuda);print(t.cuda.is_available());print(t.cuda.get_device_name(0));x=t.ones(1,device='cuda');print(x,x.device)
```

当前已跑通的基线服务：

```cmd
docker run -d --name vllm-omni-smoke --gpus all ^
  -e HF_HUB_DISABLE_XET=1 ^
  -v D:\HFModels\hf-cache:/root/.cache/huggingface ^
  -p 8091:8091 ^
  --ipc=host ^
  vllm/vllm-omni:v0.22.0-x86_64 ^
  vllm serve Qwen/Qwen2.5-0.5B-Instruct --port 8091 --gpu-memory-utilization 0.70 --max-num-seqs 1
```

接口验证：

```cmd
curl -sS http://localhost:8091/v1/models
```

PowerShell 发 chat 请求，避免 `cmd.exe` JSON 引号问题：

```powershell
$body = @{
  model = 'Qwen/Qwen2.5-0.5B-Instruct'
  messages = @(@{ role = 'user'; content = 'Say hello in one sentence.' })
  max_tokens = 32
  temperature = 0
} | ConvertTo-Json -Depth 6
Invoke-RestMethod -Uri 'http://localhost:8091/v1/chat/completions' -Method Post -ContentType 'application/json' -Body $body
```

#### 问题-原因-修复

- 问题：`docker version` 初始找不到 Docker daemon。
  - 原因：Docker Desktop 未运行。
  - 修复：用 `Start-Process` 启动 Docker Desktop；随后 Docker Engine 29.4.0 可用。
- 问题：`vllm/vllm-omni:v0.24.0` 拉取失败。
  - 原因：Docker Hub 当前没有该 tag。
  - 修复：改用 `vllm/vllm-omni:v0.22.0-x86_64`；`latest` 与它是同一 amd64 镜像。
- 问题：Hugging Face 下载初始卡在 Xet/CAS 路径。
  - 原因：缓存中出现 `.incomplete` 和 `xet`，容器日志停在下载阶段。
  - 修复：启动容器时增加 `-e HF_HUB_DISABLE_XET=1`。
- 问题：`Tongyi-MAI/Z-Image-Turbo` 和 SDXL 不适合本地小模型冒烟。
  - 原因：仓库体量分别约 54GB 和 77GB，下载和显存成本都不符合 5070 Ti 16GB 的低成本验证目标。
  - 修复：改测更小的 `inclusionAI/Ming-omni-tts-0.5B`。
- 问题：`inclusionAI/Ming-omni-tts-0.5B --omni` 下载完成后退出。
  - 原因：worker 报 `ValueError: Model class BailingMMNativeForConditionalGeneration not found in diffusion model registry.` 当前镜像 registry 不认识该模型类。
  - 修复：本轮未修复；需要等待/构建包含该类的新 vLLM-Omni 镜像，或改用当前镜像 registry 已支持且体量可接受的模型。
- 问题：`Qwen/Qwen2.5-0.5B-Instruct --omni` 也退出。
  - 原因：`--omni` 将其按 diffusion stage 处理，报 `Qwen2ForCausalLM not found in diffusion model registry.` 普通 LLM 不能作为 `--omni` 冒烟模型。
  - 修复：去掉 `--omni`，只作为同一镜像的 vLLM Docker/GPU/API 基线验证。
- 问题：Windows `cmd.exe` 下 `curl -d "{...}"` 请求 chat API 被拆坏。
  - 原因：JSON 引号转义不稳定，导致服务端收到错误 Content-Type/Body。
  - 修复：使用 PowerShell 构造 JSON 并 `Invoke-RestMethod`。

#### 验证方式

- 已通过：宿主 `nvidia-smi` 能看到 RTX 5070 Ti。
- 已通过：`docker run --rm --gpus all ... nvidia-smi` 能在容器内看到 GPU。
- 已通过：vLLM-Omni 镜像内 PyTorch 能识别 GPU 并创建 CUDA tensor。
- 已通过：同一镜像的非 `--omni` vLLM 服务监听 `8091`，`GET /v1/models` 返回 `Qwen/Qwen2.5-0.5B-Instruct`。
- 已通过：`POST /v1/chat/completions` 返回文本：`Hello! It's always nice to say hello...`
- 已观察：服务运行时宿主 `nvidia-smi` 显示显存占用约 14GB；vLLM 日志显示 `--gpu-memory-utilization=0.70` 下可用 KV cache 约 9.87GiB。

#### 最终状态

- 本地 Docker + GPU 透传 + vLLM 服务基线已跑通，当前容器 `vllm-omni-smoke` 仍在运行，端口为 `http://localhost:8091`。
- 本地 `--omni` 图像/音频小模型冒烟未跑通：主要阻塞是当前公开镜像 tag 与模型 registry/模型体量不匹配，不是 5070 Ti CUDA 链路失败。
- 云端多 GPU stage 分配尚未执行；后续应在云服务器上复用 Docker 命令，并优先选择当前镜像明确支持、体量适配云端显存的模型验证。
- 停止当前本地服务可执行：

```cmd
docker stop vllm-omni-smoke
```

### 17:20 vLLM-Omni WSL2 原生 `--omni` 小模型冒烟实测

#### 目标

- 在本地 Windows + WSL2 Ubuntu + RTX 5070 Ti 16GB 上，不走 Docker，验证 vLLM-Omni `--omni` 服务是否能实际启动并完成一次小模型接口推理。
- 本轮小模型使用 `inclusionAI/Ming-omni-tts-0.5B`，目标接口为 `POST /v1/audio/speech`。

#### 环境

- 已验证：
  - WSL 发行版：`Ubuntu`，WSL2，Ubuntu 24.04.1 LTS。
  - GPU：NVIDIA GeForce RTX 5070 Ti，WSL 内可见，显存 16303 MiB。
  - Python：系统 `/usr/bin/python3.12`，版本 3.12.3。
  - `uv`：0.11.27，安装在 `/home/fanyang/.local/bin/uv`。
  - Python venv：`/home/fanyang/work/vllm-omni-wsl/.venv`。
  - 包版本：`vllm==0.24.0`、`vllm-omni==0.24.0`、`torch==2.11.0+cu130`。
  - CUDA 验证：WSL venv 内 `torch.cuda.is_available()` 为 `True`，可在 `cuda:0` 创建 tensor。
- 未验证：
  - 本地 WSL2 跑图像模型 `Tongyi-MAI/Z-Image-Turbo` 未执行；该模型体量不适合作为 16GB 5070 Ti 的低成本优先冒烟目标。
  - 云端多 GPU stage 放置未在本轮执行。

#### 目录规划

- WSL 工作目录：`/home/fanyang/work/vllm-omni-wsl`
- WSL Hugging Face 缓存：`/home/fanyang/hf-cache`
- 模型缓存来源：从 Windows `D:\HFModels\hf-cache\hub\models--inclusionAI--Ming-omni-tts-0.5B` 复制到 WSL ext4 缓存，避免直接在 `/mnt/d` 上加载。
- 输出文件：`D:\Work\ML\ming_wsl_smoke.wav`

#### 镜像/依赖

- 本轮不使用 Docker 镜像，改走 WSL2 原生 Python。
- 安装命令：

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
mkdir -p ~/work/vllm-omni-wsl ~/hf-cache
cd ~/work/vllm-omni-wsl
~/.local/bin/uv venv --python 3.12 --seed
source .venv/bin/activate
uv pip install vllm==0.24.0 --torch-backend=auto
uv pip install vllm-omni
```

- 运行时还需要 Python 开发头文件，否则 Triton 首次推理编译会缺 `Python.h`：

```cmd
wsl -d Ubuntu -u root -- apt-get update
wsl -d Ubuntu -u root -- apt-get install -y python3.12-dev
```

#### 模型来源

- 使用模型：`inclusionAI/Ming-omni-tts-0.5B`
- WSL 本地缓存大小约 3.1GB，snapshot 中有 `model.safetensors`。
- `vllm-omni==0.24.0` 能识别 `MingTTSForConditionalGeneration`；这修复了 Docker `v0.22.0-x86_64` 中 `BailingMMNativeForConditionalGeneration not found in diffusion model registry` 的阻塞。

#### 关键命令

启动服务：

```powershell
wsl -d Ubuntu -- bash -lc 'cd ~/work/vllm-omni-wsl; export HF_HOME=$HOME/hf-cache; export HF_HUB_DISABLE_XET=1; export HF_HUB_OFFLINE=1; export TRANSFORMERS_OFFLINE=1; export VLLM_USE_FLASHINFER_SAMPLER=0; export PYTHONUNBUFFERED=1; exec /home/fanyang/work/vllm-omni-wsl/.venv/bin/vllm serve inclusionAI/Ming-omni-tts-0.5B --omni --port 8091 --gpu-memory-utilization 0.55 --max-num-seqs 1 --max-model-len 4096 --max-num-batched-tokens 4096'
```

接口验证：

```cmd
curl -sS http://localhost:8091/v1/models
```

PowerShell 生成 WAV，避免 `cmd.exe` JSON/二进制输出转义问题：

```powershell
$body = @{
  model = 'inclusionAI/Ming-omni-tts-0.5B'
  input = 'Hello, this is a vLLM Omni WSL2 smoke test.'
  response_format = 'wav'
  max_new_tokens = 256
} | ConvertTo-Json -Depth 5

Invoke-WebRequest -Uri 'http://localhost:8091/v1/audio/speech' `
  -Method Post `
  -ContentType 'application/json' `
  -Body $body `
  -OutFile 'D:\Work\ML\ming_wsl_smoke.wav'
```

#### 问题-原因-修复

- 问题：此前“`--omni` 小模型冒烟未跑通”指的是 Docker Desktop Linux 容器路径，不是 WSL2 原生 Python 路径。
  - 原因：Docker 使用公开 `v0.22.0-x86_64` 镜像，模型 registry 不认识 Ming/Bailing 类。
  - 修复：WSL2 原生安装 `vllm==0.24.0` + `vllm-omni==0.24.0`。
- 问题：Docker 容器占用约 14GB 显存，影响 WSL2 原生测试。
  - 原因：之前的 `vllm-omni-smoke` Docker 基线服务仍在运行。
  - 修复：执行 `docker stop vllm-omni-smoke`，WSL 基线显存回落到约 2.8GB。
- 问题：第一次 WSL2 原生 `--omni` 启动在 sampler 处失败。
  - 原因：FlashInfer sampler 与当前环境组合不稳定。
  - 修复：启动前设置 `VLLM_USE_FLASHINFER_SAMPLER=0`。
- 问题：第一次真实 `/v1/audio/speech` 推理返回 500，日志报 `fatal error: Python.h: No such file or directory`。
  - 原因：Triton 首次推理需要编译 CUDA helper，WSL 系统缺 `python3.12-dev`。
  - 修复：用 WSL root 安装 `python3.12-dev`；后续不再出现该编译错误。
- 问题：重启时 Hugging Face 在线 HEAD 请求 `model.safetensors.index.json` 因 SSL EOF 失败。
  - 原因：网络请求未能拿到“索引文件不存在”的正常结果，虽然本地缓存中已有 `model.safetensors` 和 `.no_exist` 记录。
  - 修复：设置 `HF_HUB_OFFLINE=1` 与 `TRANSFORMERS_OFFLINE=1`，日志显示模型 ID 被替换成本地 snapshot 路径。
- 问题：复杂 `bash -lc` 或 Python one-liner 通过 `cmd.exe` 时容易被多层引号污染。
  - 原因：Windows `cmd.exe`、WSL、bash、JSON/Python 字符串叠加转义不稳定。
  - 修复：简单命令优先 `cmd.exe`；复杂 `bash -lc` 和 JSON 请求使用 PowerShell 单引号或对象转 JSON。
- 观察：`vllm.third_party.deep_gemm` 因找不到 CUDA home 报 warning。
  - 结论：本轮对 Ming TTS 冒烟非致命，服务启动和推理均成功。

#### 验证方式

- 已通过：WSL `nvidia-smi` 能看到 RTX 5070 Ti。
- 已通过：venv 中 PyTorch 能在 `cuda:0` 创建 tensor。
- 已通过：`vllm serve ... --omni` 启动，日志显示 `Orchestrator ready with 2 stages`，支持任务包含 `{'generate', 'speech'}`。
- 已通过：`GET /v1/models` 返回 `inclusionAI/Ming-omni-tts-0.5B`。
- 已通过：`POST /v1/audio/speech` 返回 HTTP 200，服务端日志显示 `status=ok`、`response_bytes=536300`。
- 已通过：输出文件 `D:\Work\ML\ming_wsl_smoke.wav` 大小 536300 字节；WSL `file` 显示为 `RIFF ... WAVE audio, Microsoft PCM, 16 bit, mono 44100 Hz`。
- 性能/资源记录：第一次 TTS 推理约 80.7 秒，生成约 6.08 秒音频；推理时显存约 `15355 / 16303 MiB`，服务停止后回落到约 `2714 / 16303 MiB`。

#### 最终状态

- WSL2 原生 vLLM-Omni `--omni` 小模型冒烟已跑通。
- 当前服务已通过 `Ctrl+C` 停止，没有留下前台 vLLM 服务占用 GPU。
- 后续如果继续做本地 5070 Ti 测试，优先沿用 Ming TTS 命令；如切回图像模型，应先评估模型仓库体量和峰值显存。
