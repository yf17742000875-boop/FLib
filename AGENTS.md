# 项目说明（AGENTS.md）

本仓库以机器学习学习与实践为主：包含 d2l 练习、视觉/基础模型项目（dinov3、FoundationPose、Cosmos 等）、笔记与脚本。本文件为各 AI 编码工具共读的项目级上下文，OpenCode、Codex CLI、Trae 等均可识别。

## 通用约定
- 一律使用简体中文回答。
- 代码注释只解释“为什么”和非显而易见的意图，不复述代码做了什么。
- 改动前先给简短计划，优先小步可验证；不擅自大范围重构。

## 对话结束笔记规则
- 本轮对话只要产生了可复用的部署、训练、下载、排错、实验、配置或项目决策，结束前必须写入 `notes/`。
- 简短事项追加到 `notes/YYYY-MM-DD.md`；完整可复现流程写成单独 runbook，例如 `notes/autodl-cosmos3-super-2x-pro6000.md`。
- 笔记必须使用简体中文，并区分：已验证结论、未验证假设、后续待办。
- 部署/排错类笔记固定包含：目标、环境、目录规划、镜像/依赖、模型来源、关键命令、问题-原因-修复、验证方式、最终状态。
- 写笔记时优先追加或局部更新，不覆盖已有内容；如 `notes/` 不存在则创建。
- 最终回复中说明笔记写入路径和本轮记录的核心结论。
